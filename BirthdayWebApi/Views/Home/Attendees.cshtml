@model IEnumerable<BirthdayWebApi.Models.InvitationModel>
@if (Model.Count() == 1)
{
    <p>You're the first one who came to party.</p>
}
else
{
    <hr />
    <p>
        @foreach (var i in Model)
        {
            <label class="label label-success">@i.Name</label>
        }
    </p>
}